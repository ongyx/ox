struct Point {
  x, y
}

func Point.distance(self) {
  return (self.x ^ 2) + (self.y ^ 2)
}

struct RelativePoint(Point) {
  cx, cy
}

// syntactically correct, but invalid (can't add structs together)
//myPoint = Point(1 + (2 + (3)), 2, 3) + Point(1,2)

p = Point(1, 2)
println(p)
